# Core dependencies for NBO package
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.1.0
pyarrow>=10.0.0

# Optional: For better performance and additional features
# fastparquet>=0.8.0  # Alternative parquet engine
# joblib>=1.2.0       # For parallel processing
